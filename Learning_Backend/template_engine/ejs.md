Html cannot do calculation/ computation

ejs is html but with computation power( javascript inside html)

setup:
1. install
- ejs install (npm install ejs)

2. configure ejs
- app.set("view engine", "ejs");

3. Make a folder- views

4. make ejs files inside that folder

5. instead os using res.send - use res.render

-- while rendering make sure to use the file ony inside the views folder and do not add .ejs


template engines are many-> pug, jade, ejs,

template engines-> a markup which wil  later be converted to html
