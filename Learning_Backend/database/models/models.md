models is usually referred to the code which is used to build the colllection (table) inside the database

schema is the code which is reflected as the documents in the database

Complete data-> database
a partof that is collections made using models
Document-> the smallest subcategory

database-> collections-> document


