install these packages
    npm i passport passport-local passport-local-mongoose mongoose express-session

write app.js
code forst in app.js file and write it after view engine and before logger

setup users.js

inindex.js try register first
